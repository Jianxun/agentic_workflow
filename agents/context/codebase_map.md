# Codebase Map

- `agents/roles/`: role definitions and operating procedures.
- `agents/context/`: shared state, contracts, and task tracking.
- `agents/scripts/`: workflow automation scripts (for example, `lint_tasks_state.py`).
- `agents/scratchpads/`: per-task working notes.
- `agents/adr/`: architecture decision records.
